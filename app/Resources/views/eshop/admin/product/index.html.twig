{% extends 'eshop/admin/base.html.twig' %}
{% block title %}List of Products {% endblock %}
    {% block title_link %} 
    <small> 
        <a href="{{path('eshop_admin_product_new')}}">Add new</a> 
    </small>
{% endblock %}  
{% block body %} 

   <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Image</th>
                <th>Label</th>
                <th>Details</th>
                <th>Price</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for product in products %}
            <tr>
                <td><a href="{{ path('eshop_admin_product_show', { 'id': product.id }) }}">{{ product.id }}</a></td>
                <td>
                    {% if product.image is not null %}
                    <img src="/uploads/products/{{ product.image }}" style="width: 50px;" /> 
                    {% endif %}
                </td>
                <td>{{ product.label }}</td>
                <td>{{ product.details | raw }}</td>
                <td>{{ product.price }}</td>
                <td>
                   
                            <a href="{{ path('eshop_admin_product_show', { 'id': product.id }) }}" class="glyphicon glyphicon-pencil"></a>
                        
                            <a href="{{ path('eshop_admin_product_edit', { 'id': product.id }) }}" class="glyphicon glyphicon-remove-circle remove-item"></a>
                   
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('eshop_admin_product_new') }}">Create a new product</a>
        </li>
    </ul>
{% endblock %}
